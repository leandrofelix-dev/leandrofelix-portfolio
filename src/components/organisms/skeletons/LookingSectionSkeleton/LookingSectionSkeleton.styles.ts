import styled from 'styled-components'

export const LookingRoot = styled.div`
  border-bottom: 1px solid var(--primary-dark);
`

export const JsonBlock = styled.div`
  display: flex;
  flex-direction: column;
  gap: 0.75rem;
  font-family: var(--font-accent);
`
